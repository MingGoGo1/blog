server:
  port: "8868"
  mode: "debug"  # debug, release

database:
  host: "localhost"
  port: "3306"
  user: "root"
  password: "123456"  # 示例密码，请修改为您的实际密码
  dbname: "blog"

redis:
  host: "localhost"
  port: "6379"
  password: ""  # 如果Redis设置了密码，请在此填写
  db: 7

minio:
  endpoint: "localhost:9000"
  access_key_id: "minioadmin"      # 示例访问密钥
  secret_access_key: "minioadmin"  # 示例密钥
  use_ssl: false
  bucket_name: "blog-files"

jwt:
  secret: "your-jwt-secret-key-change-in-production"  # 生产环境请务必修改此密钥
  expire_hour: 24
